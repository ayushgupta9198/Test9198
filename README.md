# home

if we are running for the first time we need to configure name and email address to commit the changes over Github

github config user.name
github config user.email 

and then just git push origin main 
add your username and password as token.
